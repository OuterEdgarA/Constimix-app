# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence for constimix_app (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-20 (commit 3633838). Confidence: 100%.
### How to work in this repo

- **Trust the index.** `verified: true` means the bytes were checked against the live tree, so never re-read those lines. Re-read only on `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"` or `confidence: "low"`; `index_behind: true` alone is informational.
- **Pre-edit, not instead-of-edit.** These tools decide *which* files to read and edit. Claude Code requires a raw Read of any file you will Edit, and that Read is correct and expected.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>` — same command, exit code preserved, errors-first output. A `[repowise#<ref>: N lines omitted]` marker is recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.

### Tools

| Tool | When and why |
|------|--------------|
| `get_answer(question)` | First call for any how / where / why question. `confidence: "high"` or `grounding: "extracted"` is content-grounded, so cite it directly; `symbol_bodies` carries full live bodies. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols: docs, signatures, hotspot, fix history. No source bytes — `include=["skeleton"]` for the whole file verified, `["callers"|"decisions"]` for depth. Batch targets. |
| `get_symbol(id)` | **Follow-up, not an entry point** — one verified body for an id a prior response named (`path.py::Name`, `path.py:140-180`, `repowise#<hex>`). Never walk a file symbol by symbol; Read it. |
| `search_codebase(query)` | Hybrid search, auto-routed by query shape; force with `mode=symbol|path|concept|hybrid`. A hit whose `sources` are `[fts]` only has no semantic agreement, so verify it. |
| `get_why(query, targets?)` | Why the code is shaped this way: decision records, git archaeology, rationale comments. Call before a refactor or a pattern divergence. |
| `get_risk(targets, changed_files?)` | What history says about touching these files. PR mode (`changed_files`) leads with a `directive`: read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first. |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Defect score for a whole commit or `base..head` range, from its diff on the live checkout. Lead with `risk_percentile`. Scores a range; `get_risk` scores paths. |
| `get_health(targets?, include?)` | Defect / maintainability / performance scores and findings. Self-check the files you touched before finishing. |
| `get_dead_code()` | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes. |
| `get_overview()` | Architecture map. Call once, first, in an unfamiliar repo; skip it after that. |

### Architecture
**Files:** 120 | **Lines:** 16903 | **Monorepo:** 2 packages
constimix_app is a multi-package cpp codebase of 120 files, split across 2 packages. Execution starts at lib/main.dart, linux/runner/main.cc, windows/runner/main.cpp and 1 other entry point. Start here when reading the codebase. Ranked by PageRank over the import graph: the files most of the codebase ultimately depends on.

### Key modules
- `windows/flutter/ephemeral/cpp_client_wrapper/include/flutter` — windows/flutter/ephemeral/cpp_client_wrapper/include/flutter
**Language:** cpp | **Files:** 26 | **Public symbols:** 255 / 274
Covers the…
- `lib/core` — lib/core/data · lib/core/models · lib/core/services
**Language:** dart | **Files:** 17 | **Public symbols:** 136 / 147
Heads the Core…
- `lib/core/data` — lib/core/data · lib/core/models
**Language:** dart | **Files:** 13 | **Public symbols:** 118 / 127
Covers the 13 source files in 2…
- `windows/runner` — windows/runner
**Language:** cpp | **Files:** 8 | **Public symbols:** 50 / 51
Covers the 8 source files in windows/runner
- `windows/flutter/ephemeral` — windows/flutter/ephemeral · windows/flutter/ephemeral/cpp_client_wrapper
**Language:** cpp | **Files:** 14 | **Public symbols:** 153 /…
- `linux/runner` — linux/runner
**Language:** cpp | **Files:** 3 | **Public symbols:** 3 / 11
Covers the 3 source files in linux/runner
- `lib/features/academics` — lib/features/academics · lib/features/admin · lib/features/auth · lib/features/community · lib/features/dashboard · lib/features/enrollment…
- `lib/core/services` — lib/core/services
**Language:** dart | **Files:** 4 | **Public symbols:** 18 / 20
Covers the 4 source files in lib/core/services
- `lib/shared/widgets` — lib/shared/widgets
**Language:** dart | **Files:** 3 | **Public symbols:** 6 / 6
Covers the 3 source files in lib/shared/widgets
- `lib` — lib · lib/app
**Language:** dart | **Files:** 3 | **Public symbols:** 5 / 6
Covers the 3 source files in 2 directories under lib

### Entry points
- `lib/main.dart`
- `linux/runner/main.cc`
- `windows/runner/main.cpp`

### Files that need care (bug-fix history first, then churn — check `get_risk` before editing)
- `.repowise/knowledge-graph.json` — 8 commits/90d
- `lib/l10n/app_localizations.dart` — 8 commits/90d
- `lib/l10n/app_localizations_es.dart` — 8 commits/90d
- `lib/features/enrollment/enrollment_wizard_screen.dart` — 6 commits/90d
- `lib/features/admin/semester_admin_screen.dart` — 3 commits/90d

### Code health
Three co-equal signals: defect risk 7.37/10 avg, hotspot health 6.29/10 (stable), worst `lib/features/enrollment/enrollment_wizard_screen.dart` at 1.0/10 · maintainability 8.52/10 · performance risk 0 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

Critical files:
- `lib/l10n/app_localizations.dart` — untested hotspot — impact −2.0
- `lib/core/data/mock_repository.dart` — untested hotspot — impact −2.0
- `lib/features/enrollment/enrollment_wizard_screen.dart` — god class (_EnrollmentWizardScreenState) — impact −1.6
- `lib/features/enrollment/enrollment_wizard_screen.dart` — brain method (_schoolDataStep) — impact −0.4
- `lib/features/enrollment/enrollment_wizard_screen.dart` — brain method (_saveEnrollment) — impact −0.2

<!-- REPOWISE:END -->
