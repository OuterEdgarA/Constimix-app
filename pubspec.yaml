name: constimix_app
description: Mobile-first school companion for Constitucion de 1917 Mixta.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: 7.0.2
  flutter_localizations:
   sdk: flutter
  intl: any
  http: 1.6.0
  latlong2: 0.9.1
  pdf: 3.13.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
